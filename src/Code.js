import React, { useState } from "react";

const codeFunction = (codeData) => {
  // Перенос строки
  // Отступы слева
  // Номерация рядов
  // Цвет кода для типов
  //// Коментарии
  // Копирование кода
};

export default Code;
